export type User = {
  name: string;
};

export type SignInCredentials = {
  email: string;
  password: string;
};

export type SignUpCredentials = {
  email: string;
  nickname: string;
  password: string;
};
